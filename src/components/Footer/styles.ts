import styled from 'styled-components'

export const FooterContainer = styled.footer`
	margin-top: auto;
  font-size: 14px;
  display: flex;
  flex-direction: row;
  align-items: center;
  position: relative;
`